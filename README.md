# RDSGN — my-website

This is a simple static website for rdsgn.in hosted on GitHub Pages.

## How to run locally
- Open `index.html` in browser
- Or use VSCode Live Server extension

## How to update
```bash
git add .
git commit -m "Update site"
git push origin main
